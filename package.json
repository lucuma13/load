{
  "name": "load-tests",
  "version": "0.1.0",
  "description": "macOS bats test tooling for the load workstation setup scripts",
  "private": true,
  "scripts": {
    "test": "bats tests",
    "test:ci": "bats --filter-tags '!live' tests"
  },
  "devDependencies": {
    "bats": "^1.13.0",
    "bats-assert": "^2.2.4",
    "bats-support": "git+https://github.com/bats-core/bats-support.git#semver:^0.3.0"
  }
}
